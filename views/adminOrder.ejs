<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Order</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <link rel="stylesheet" href="/css/style.css"> <!-- Add your custom styles here -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #e6f7ff; /* Light Blue */
        }

        header {
            background-image: linear-gradient(90deg, #020024 0%, #090979 35%, #00d4ff 100%);
            color: #fff;
            text-align: center;
            padding: 10px 0;
        }

        .container {
            margin-top: 20px;
        }

        .address-card {
            border: 1px solid #b3d9ff; /* Lighter Blue */
            border-radius: 10px;
            margin-bottom: 20px;
            padding: 20px;
            background-color: #ffffff; /* White */
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }

        .address-card h5 {
            color: #005580; /* Darker Blue */
            margin-bottom: 10px;
        }

        .address-details p {
            margin: 5px 0;
        }

        .btn-action {
            margin-top: 10px;
        }

        /* Different button styling */
        .btn-info,
        .btn-danger {
            background-color: #005580;
            color: #fff;
            border: none;
            border-radius: 5px;
            padding: 10px 20px;
            text-decoration: none;
        }

        .btn-info:hover,
        .btn-danger:hover {
            background-color: #003366;
        }

        /* Add style for the top-left aligned button */
        .add-address-btn {
            margin-top: 20px;
            margin-bottom: 20px;
            margin-left: 20px;
        }
        body, h1, h2, p, table {
    margin: 0;
    padding: 0;
}

/* Apply a simple border-collapse to the table */
.table-container table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}

/* Style table headers */
.table-container th {
    background-color: #f2f2f2;
    padding: 10px;
    text-align: left;
    font-weight: bold;
}

/* Style table data cells */
.table-container td {
    border: 1px solid #ddd;
    padding: 10px;
}

/* Highlight the table rows on hover */
.table-container tbody tr:hover {
    background-color: #f5f5f5;
}

/* Responsive design considerations */

/* Make the table responsive */
@media screen and (max-width: 600px) {
    .table-container table {
        overflow-x: auto;
    }
}

/* Stack the table headers on smaller screens */
@media screen and (max-width: 400px) {
    .table-container th {
        display: block;
    }
    .table-container td {
        display: block;
        text-align: left;
    }
    .table-container th, .table-container td {
        width: 100%;
        box-sizing: border-box;
    }
}
@media screen and (max-width: 768px) {
    header {
        font-size: 1.5rem;
    }
    .table-container table {
        font-size: 0.8rem;
    }
}
        .btn-dropdown {
    background-color: transparent;
    color: inherit;
    border: 1px solid #ccc; /* Add your desired border color */
    border-radius: 5px;
    padding: 0.375rem 0.75rem; /* Adjust padding as needed */
}

/* Remove default background color on hover */
.cancel-btn {
        background-color: #097615; /* Green color */
        color: #fff;
        border: none;
        border-radius: 5px; /* Curved corners */
        padding: 10px 20px;
        cursor: pointer;
    }

    /* Hover effect for the "Update" button */
    .cancel-btn:hover {
        background-color: #078109; /* Darker green on hover */
    }
        
    </style>
     
</head>

<body>
   
    <header>
        <h1> OrderManagement</h1>
    </header>
    <div class="container">
        <a href="/adminDashboard" class="btn btn-info add-address-btn">Back</a>
        <div class="row">
            <div class="col-md-12">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>User Name</th>
                                <th>Product Name</th>
                                <th>Quantity</th>
                                <th>Total Prices</th>
                                <th>Order Date</th>
                                <th>Payment Method</th>
                                <th>Status</th>
                                <th>Action</th>
                            </tr>
                        </thead>

                        <tbody>
                            <% orderdetalist.forEach((order, index) => { %>
                                <% order.productcollection.forEach((product, productIndex) => { %>
                                    <tr>
                                        <td data-label="User Name"><%= order.address.firstname %></td>
                                        <td data-label="Product Name"><%= product.productName %></td>
                                        <td data-label="Quantity"><%= product.quantity %></td>
                                        <td data-label="Total Prices">₹<%= product.price * product.quantity %></td>
                                        <td data-label="Order Date"><%= order.orderDate %></td>
                                        <td data-label="Payment Method"><%= order.paymentMethod %></td>
                                        <td data-label="Status">
                                            <% if (product.status === 'Delivered' && product.status !== 'Cancelled') { %>
                                                <%= product.status %>
                                            <% } else { %>
                                                <form action="/update-status/<%= product._id %>/<%= order._id %>" method="post">
                                                    <div class="dropdown">
                                                        <select name="status" class="form-select" aria-label="Select Status">
                                                            <option value="pending" <%= product.status === 'pending' ? 'selected' : '' %>>Pending</option>
                                                            <option value="Shipped" <%= product.status === 'Shipped' ? 'selected' : '' %>>Shipped</option>
                                                            <option value="Cancelled" <%= product.status === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                                                            <option value="Delivered" <%= product.status === 'Delivered' ? 'selected' : '' %>>Delivered</option>
                                                        </select>
                                                    </div>
                                                    <input type="hidden" name="productid" value="<%= product._id %>">
                                                    <td data-label="Action">
                                                        <% if (product.status !== 'Cancelled') { %>
                                                            <button type="submit" class="cancel-btn">Update</button>
                                                        <% } %>
                                                    </td>
                                                </form>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% }); %>
                        </tbody>
                        </table>
                </div>
            </div>
        </div>
    </div>

    
    <script>
        

        const hamburgerMenu = document.getElementById("hamburger-icon");
        const link = document.querySelectorAll(".link");
        const closeBtn = document.getElementById("close");

        for (let i = 0; i < link.length; i++) {
            link[i].addEventListener("click", function () {
                var current = document.getElementsByClassName("active");
                current[0].className = current[0].className.replace(" active", "");
                this.className += " active";
            });
        }

        hamburgerMenu.addEventListener("click", function () {
            const sidebar = document.getElementById("sidebars");
            sidebar.classList.toggle("toggle");
        });

        function closeNav() {
            const sidebar = document.getElementById("sidebars");
            sidebar.classList.toggle("toggle");
        }
    </script>

   
</body>

</html>

   

  